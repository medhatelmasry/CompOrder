@model Contact
@{
    ViewData["Title"] = "Contact Summary";
}
<h1>@ViewData["Title"]</h1>

<table>
    <tr>
        <td>Email:</td>
        <td>@Model.Email</td>
    </tr>
    <tr>
        <td>Full Name:</td>
        <td>@Model.FullName</td>
    </tr>
</table>
